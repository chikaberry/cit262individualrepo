#© 2021 Sean Murdock

apiVersion: apps/v1
kind: Deployment
metadata:
  name: chika-stedi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chika-stedi
  template:
    metadata:
      labels:
        app: chika-stedi
    spec:
      containers:
        - name: chika-stedi
          image: gcr.io/gcr.io/funky-flex/stedi-chikaberry:latest
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: ".5"
              memory: "500Mi"
            requests:
              cpu: ".25"
              memory: "250Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 3